import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';
import { RouterOutlet } from '@angular/router';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [],
  template: '',
  styles: '',
})
export class AppComponent {
  title = 'ng-unified-control-change-state-events-demo';
}
